{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/globalmindsolutions/acs/plugins/acs/schemas/settings.schema.json",
  "title": "acs settings.json",
  "description": "Configuration generated by /init. Scopes: ~/.acs/settings.json (user), <repo>/.acs/settings.json (project, committed), <repo>/.acs/settings.local.json (machine-specific, gitignored — holds workspace_path). Per-key merge: local -> project -> user. Unknown keys are tolerated for forward compatibility.",
  "type": "object",
  "$defs": {
    "roleModel": {
      "oneOf": [
        {"type": "string", "minLength": 1},
        {
          "type": "object",
          "properties": {
            "model": {"type": "string", "minLength": 1},
            "effort": {"enum": ["low", "medium", "high", "xhigh", "max", "inherit"]}
          },
          "additionalProperties": false
        }
      ]
    }
  },
  "properties": {
    "workspace_path": {
      "description": "Absolute path of the workspace folder. Required at init time; MUST be outside the consumer repo (worktrees and parallel tickets depend on it). Machine-specific: lives in settings.local.json.",
      "type": "string",
      "minLength": 1
    },
    "ticket_prefix": {
      "description": "Per-repo prefix for ticket ids (<prefix>-<n>). Required at init time; no global default.",
      "type": "string",
      "pattern": "^[A-Z][A-Z0-9]*$"
    },
    "test_coverage_percent": {
      "description": "Coverage target for the /code TDD cycle. Missing the target is a hard fail.",
      "type": "number",
      "exclusiveMinimum": 0,
      "maximum": 100,
      "default": 90
    },
    "merge_strategy": {"enum": ["squash", "merge", "rebase"], "default": "squash"},
    "prd_path": {"type": "string", "default": "docs/product"},
    "architecture_path": {"type": "string", "default": "docs/architecture"},
    "requirements_path": {
      "description": "Living requirements doc set — the standing behavioral contract, one file per feature area, accumulated ticket by ticket by /code's documentation step; read by /create-ticket and /create-spec as the area's current behavior.",
      "type": "string",
      "default": "docs/requirements"
    },
    "adr_path": {
      "description": "Where /code commits accepted decision records from each ticket's design.md (default-on so decisions outlive archived ticket partitions). Explicit null disables — designs then stay workspace-only.",
      "oneOf": [{"type": "string", "minLength": 1}, {"type": "null"}],
      "default": "docs/adr"
    },
    "e2e": {
      "description": "End-to-end test layer (unset = repo has no e2e suite). When configured: /create-spec test plans must state e2e impact, /code authors/updates e2e tests in the same changeset, and the code-verifier runs the suite (green required for a passing verdict; per_iteration=false runs it only on the final, otherwise-passing iteration since e2e is slow). /create-project scaffolds the harness for greenfield repos with a user-facing surface.",
      "type": "object",
      "required": ["command"],
      "properties": {
        "command": {"type": "string", "minLength": 1, "description": "Command that runs the e2e suite, e.g. 'npm run test:e2e'."},
        "setup": {"type": "string", "minLength": 1, "description": "Optional environment bring-up, e.g. 'docker compose up -d --wait'."},
        "teardown": {"type": "string", "minLength": 1, "description": "Optional environment teardown; always run after the suite, pass or fail."},
        "per_iteration": {"type": "boolean", "default": false, "description": "true = verifier runs e2e every iteration; false = only on the final, otherwise-passing iteration."}
      },
      "additionalProperties": true
    },
    "models": {
      "description": "Per-role subagent model and reasoning effort. Resolution per field: overrides.<skill>.<role> -> <role> -> inherit. coordinator applies only to /ship-spawned coordinators.",
      "type": "object",
      "properties": {
        "planner": {"$ref": "#/$defs/roleModel"},
        "executor": {"$ref": "#/$defs/roleModel"},
        "verifier": {"$ref": "#/$defs/roleModel"},
        "coordinator": {"$ref": "#/$defs/roleModel"},
        "overrides": {
          "type": "object",
          "propertyNames": {
            "enum": ["create-prd", "create-architecture", "create-project", "create-ticket", "create-design", "create-spec", "code", "create-pr", "merge-pr", "ship"]
          },
          "additionalProperties": {
            "type": "object",
            "properties": {
              "planner": {"$ref": "#/$defs/roleModel"},
              "executor": {"$ref": "#/$defs/roleModel"},
              "verifier": {"$ref": "#/$defs/roleModel"},
              "coordinator": {"$ref": "#/$defs/roleModel"}
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "tracker": {
      "description": "Ticket tracking backend. Tickets are always local-first; github/jira adds two-way sync via the gh / acli CLIs (which manage their own auth — no secrets in settings).",
      "type": "object",
      "properties": {
        "provider": {"enum": ["local", "github", "jira"], "default": "local"},
        "github": {
          "type": "object",
          "properties": {
            "owner": {"type": "string"},
            "project_number": {"type": "integer", "minimum": 1}
          },
          "additionalProperties": true
        },
        "jira": {
          "type": "object",
          "properties": {
            "base_url": {"type": "string"},
            "project_key": {"type": "string"},
            "board": {"type": "string"}
          },
          "additionalProperties": true
        }
      },
      "additionalProperties": true
    },
    "formats": {
      "description": "Formats for generated artifacts. Inline fields use {placeholder} syntax; an unknown placeholder is a validation error (pre-hooks exit 2). branch_name MUST embed {ticket_id}. Description templates are referenced by built-in name (pr-default, epic-default, story-default, task-default), repo-relative name under .acs/templates/, or absolute path.",
      "type": "object",
      "properties": {
        "branch_name": {"type": "string", "pattern": ".*\\{ticket_id\\}.*", "default": "{type}/{ticket_id}-{slug}"},
        "commit_message": {"type": "string", "default": "{ticket_id} {summary}"},
        "pr_title": {"type": "string", "default": "[{ticket_id}] {title}"},
        "pr_description_template": {"type": "string", "default": "pr-default"},
        "tickets": {
          "type": "object",
          "propertyNames": {"enum": ["epic", "story", "task"]},
          "additionalProperties": {
            "type": "object",
            "properties": {
              "title": {"type": "string"},
              "description_template": {"type": "string"}
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": true
}
