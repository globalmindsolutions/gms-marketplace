{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/globalmindsolutions/acs/plugins/acs/schemas/session-pointer.schema.json",
  "title": "acs sessions/<checkout-id>.json",
  "description": "Per-checkout current-ticket pointer, written by skill-start at skill start. Hooks (deterministic scripts that cannot read conversation history) resolve the current ticket from it, falling back to the branch name. One pointer per repo checkout/worktree, so parallel sessions never clash.",
  "type": "object",
  "required": ["checkout_id", "ticket_id"],
  "properties": {
    "checkout_id": {"type": "string", "minLength": 1},
    "checkout_path": {"type": "string"},
    "ticket_id": {"type": "string", "pattern": "^[A-Z][A-Z0-9]*-[0-9]+$"},
    "skill": {"type": "string"},
    "updated_at": {"type": "string", "format": "date-time"}
  },
  "additionalProperties": true
}
